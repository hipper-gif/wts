# uploads/ ディレクトリへの直接アクセスを全面禁止
# ファイルは必ずPHP経由（認証付き）で配信する
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
